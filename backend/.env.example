# ========================================
# BACKEND ENVIRONMENT VARIABLES
# ========================================
# IMPORTANT: These are example values. NEVER commit actual secrets to Git!
# Set real values in Render environment variables dashboard.

# Server Configuration
PORT=5000
NODE_ENV=production

# Database
# Production: Use MongoDB Atlas connection string with NEW password (rotate after leak)
# Format: mongodb+srv://username:password@cluster.mongodb.net/dbname
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vlogsphere

# Frontend URL (for CORS, email links, redirects)
# Production: https://vlogspherefrontend.vercel.app
# Development: http://localhost:3000
FRONTEND_URL=https://vlogspherefrontend.vercel.app

# CORS Origins (comma-separated list)
# Production: Include both production frontend and localhost for dev
CORS_ORIGINS=https://vlogspherefrontend.vercel.app,http://localhost:3000

# JWT Secrets
# CRITICAL: Generate new secrets using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# ⚠️ NEVER use these example values in production!
JWT_SECRET=your_jwt_secret_here_generate_new_64_byte_hex
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here_generate_new_64_byte_hex
JWT_EXPIRE=7d
JWT_REFRESH_EXPIRE=30d

# Cloudinary (Image Storage)
# Get these from: https://cloudinary.com/console
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Redis (Optional - for caching)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Email Configuration (Resend)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=VlogSphere

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100

# Upload Settings
MAX_FILE_SIZE=5242880  # 5MB in bytes
UPLOAD_PATH=./uploads

# AI/ML Configuration
AI_TAGGING_ENABLED=true
MIN_DESCRIPTION_LENGTH=50

# ========================================
# DEPLOYMENT INSTRUCTIONS - RENDER
# ========================================
#
# 1. Go to Render Dashboard → Your Service → Environment
# 2. Add the following environment variables:
#
#    CRITICAL (Generate new values):
#    - MONGODB_URI (with NEW password)
#    - JWT_SECRET (new 64-byte hex)
#    - JWT_REFRESH_SECRET (new 64-byte hex)
#    - CLOUDINARY_API_SECRET (regenerate in Cloudinary dashboard)
#    - RESEND_API_KEY (create new key in Resend)
#
#    URLs:
#    - FRONTEND_URL=https://vlogspherefrontend.vercel.app
#    - CORS_ORIGINS=https://vlogspherefrontend.vercel.app,http://localhost:3000
#
#    Other:
#    - NODE_ENV=production
#    - PORT=5000
#
# 3. Click "Save Changes" and Render will auto-redeploy
#
# ========================================
# SECURITY CHECKLIST
# ========================================
# ✅ .env is in .gitignore
# ✅ All secrets rotated after repository cleanup
# ✅ MongoDB user password changed
# ✅ JWT secrets regenerated
# ✅ Cloudinary API secret regenerated
# ✅ Resend API key regenerated
# ✅ No secrets stored in code or repository
# ✅ Secrets exist ONLY in deployment platform (Render)
#
